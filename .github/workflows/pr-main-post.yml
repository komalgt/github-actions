# Code Generated by Sidekick is for learning and experimentation purposes only.
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Add your custom steps below this line
      # For example, you can add steps for Python, Java, or shell scripts
      # - name: Run a script
      #   run: echo "This is where you run your scripts or commands."

# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Simple Email Alert

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Always fail (for demo)
        run: exit 1

  email_notify:
    needs: build
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: komalgt@gmail.com
          password: rkuz ippx ackk cwmv
          to: kturakannavar@deloitte.com
          from: komalgt@gmail.com.com
          subject: 'GitHub Actions Failed'
          body: 'The workflow has failed. Please check it.'

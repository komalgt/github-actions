# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Example Workflow With Exposed Secrets

on:
  push:
    branches: [main]
  pull_request:

jobs:
  insecure-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set plaintext secret ENV
        env:
          DB_PASSWORD: supersecret123  # <-- Hardcoded secret!
          API_KEY: abcd1234-efgh5678   # <-- Hardcoded secret!
        run: |
          echo "Using secrets: $DB_PASSWORD, $API_KEY"

      - name: Use secret in command
        run: |
          echo "API Key is abcd1234-efgh5678"  # Secret echoed directly!

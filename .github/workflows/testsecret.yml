# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Secret Test Workfloww

on:
  push:

jobs:
  testsec:
    runs-on: ubuntu-latest
    steps:
      - name: testing secrets
        env:
          Devops: ${{ secrets.SECRET_NAME }}
        run: |
          echo "$Devops..."
          echo "$Devops"

      - name: environment variables
        run: |
          echo "More environment variable usage here"
